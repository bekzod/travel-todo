import {
  dateDuration
} from 'front/helpers/date-duration';

module('DateDurationHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = dateDuration(42);
  ok(result);
});
