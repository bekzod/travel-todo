import {
  formateDate
} from 'front/helpers/formate-date';

module('FormateDateHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formateDate(42);
  ok(result);
});
