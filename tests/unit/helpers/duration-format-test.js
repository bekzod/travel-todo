import {
  durationFormat
} from 'front/helpers/duration-format';

module('DurationFormatHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = durationFormat(42);
  ok(result);
});
